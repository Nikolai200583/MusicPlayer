import { RegistrationForm } from '../../components/LoginForm/RegistrationForm';

export const Registration = () => {
    return (
        <div>
            <RegistrationForm />
        </div>
    );
};
